# Dependencies
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Environment
.env

# Generated data (re-created by running the pipeline)
# data/raw/combine_*.csv
# data/raw/draft_picks_*.csv
# data/calibration_set.json
# data/prospects_rated.json

# EA binary files (copyright — download via script 2)
data/raw/CAREERDRAFT-2025_M26

# Output (generated artifact — share separately)
data/output/

# Checkpoint files (resume state)
data/checkpoint_*.json

# Pipeline run logs
data/raw/*.log
data/raw/*.err
data/raw/_*.html

# Hand-saved backup snapshots before destructive runs
data/*_backup_*.json
data/raw/CAREERDRAFT-NFLDRAFT2026

# Per-rookie diagnostic dumps from extractRookieRatings.mjs (regenerable)
# data/full_solution_2_ratings.json
# data/rookie_ratings_from_franchise.json
# data/rookie_ratings_post_madden.json
# data/rookie_ratings_post_fix.json

# franchise_ratings.json IS tracked — the OG predictor depends on it
# being present alongside the other pre-fetched data files.

# Claude-Code transient lock
.claude/scheduled_tasks.lock

# Rookie-visuals pipeline cache (regenerate via 9n/9o/9p — see commands.md)
data/raw/headshots/
data/raw/headshots_calibration/
data/raw/headshots_debug/
data/raw/headshot_manifest.json
data/raw/headshot_manifest_calibration.json
data/raw/skin_tone_measurements.json
data/raw/vet_skin_measurements.json
# 3.7 MB MediaPipe model — auto-downloaded by 9o
data/raw/face_landmarker.task

# OS
.DS_Store
